import { Card } from "react-bootstrap";
import { Fragment } from "react/jsx-runtime";

export function Auctions(user_id: any) {
  return (
    <Fragment>
      <Card></Card>
    </Fragment>
  );
}

export default Auctions;
